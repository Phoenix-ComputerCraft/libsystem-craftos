return {
    expect = require "system.expect",
    filesystem = require "system.filesystem",
    framebuffer = require "system.framebuffer",
    graphics = require "system.graphics",
    hardware = require "system.hardware",
    keys = require "system.keys",
    log = require "system.log",
    network = require "system.network",
    pretty = require "system.pretty",
    process = require "system.process",
    serialization = require "system.serialization",
    terminal = require "system.terminal",
    util = require "system.util"
}